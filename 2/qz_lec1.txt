почему лучше не менять имя папки static через аргумент Flask	программы-раздатчики статики( н-р nginx) настроены так чтобы работать с параметрами по умолчанию

моржовый оператор :=	присваивает значение справа в переменную слева и может в этой же строке с ней работать не поднимая NameError

зачем конструкция @app.route("/",methods=['POST','GET'])	чтобы функция обрабатывала как POST так и GET запросы по адресу

порядок обработки в конструкции @app.route("/",methods=['POST','GET'])	сначала пользователь переходя по ссылке получает по GET форму к заполнению; заполнив форму и нажав отправить- отправляет post запрос

переменная для страницы запрошенной пользователем	flask.request.base_url

что ложим в @app.error_handler(?) 	код обрабатываемой ошибки

возврат @app.error_handler(error_code) 	return render_template('page',**vars), error_code

куда может вести страничка "попробуйте позже"	на эту же страницу, с идеей что к моменту обновления страницу починят

где в отладчике смотреть установленные cookies 	application>сookies(слева)>сайт

нюанс по cookies(доступ) 	доступно для изменения пользователем

где в отладчике смотреть содержимое request.headers 	network>{сайт}>response headers

зачем в <form></form> аттрибут enc-type="multipart/form-data"   чтобы сработала загрузка файла

что такое аттрибут action в <form>, как его оформлять  ссылка,фреймворком через url_for как и любую другую ссылку